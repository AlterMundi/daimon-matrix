# DM-082 relationship runtime self-audit

Date: 2026-08-05

Verdict: **CANDIDATE READY FOR FULL CI; NO LIVE CONSENT, HOST MUTATION OR
EXTERNAL MESSAGE AUTHORIZED**.

## Scope and review gate

This audit covers issue #109 under accepted signed claim
`c90f38d8-b966-43cb-bdbc-307401b4b8bf` on branch
`dm-082-relationships`. CI plus this explicit adversarial audit are the review
gate requested by the issue; no recursive external review loop is required.

The candidate implements signed cards, bilateral relationship handshakes,
founded-Tribe membership and founder succession, resource grants and strict
delegation, revocation/relinquishment, observer-local durable reduction,
verified DM-054 snapshots, hosted V6, daemon/CLI/MCP methods, generated vectors
and an installed synthetic journey.

## Findings resolved during audit

- Membership acceptance initially had no predecessor-linked re-entry series.
  It now carries an exact sequence and prior terminal reference.
- Multiple accepted invitations or competing leave/expulsion events could have
  depended on iteration order. They now quarantine the member lane without a
  winner, and reversed-input tests produce the same report.
- Package and reproducible-build allowlists omitted the new runtime modules.
  All source, sdist and wheel inventories now name them explicitly.
- The first conformance insertion was not closed against the compiled scenario
  set and contained unsorted lists. The suite version, compiled IDs and pinned
  canonical digest now advance together.
- The original work lacked deterministic negative vectors and a generated
  scenario map. Both are now release-blocking and checked for byte drift.
- The first synthetic path proved grant state but not its consumption boundary.
  It now converts the closed disclosure result and selected DM-054 membership
  target into DM-051 authorization and a signed DM-052 relationship leg.
- DM-053 originally compared a stable relationship recipient ID directly with
  sealed embodiment IDs. It now retains the semantic ID while checking the
  leg's exact receipt-origin embodiment at carrier intake.
- Route acceptance could have been mistaken for delivery in the first draft.
  The installed journey proves the leg remains accepted after route ACK and
  becomes delivered only after verifying the recipient being's signed receipt.
- Recipient opening originally trusted the frozen authorization after a later
  grant terminal. Intake now re-evaluates current relationship disclosure;
  stale direct replay and hub forwarding are refused before another private
  open.
- A local one-being ledger could not verify the foreign receipt. The explicit
  root-authority inventory now selects exact known roots for foreign event
  verification while retaining one local root for mutation and transport.
- A corrupt SQLite header could leave the connection allocated while setup
  failed. Connection initialization now closes on every exceptional path.

## Authority and privacy conclusions

- Being-root authority, not a hostname, account, card route or Cluster row,
  verifies every author.
- Relationship, membership and grant remain independent facts.
- Unknown, stale, incomplete, forked, widened, unaccepted or terminal evidence
  cannot enter a verified snapshot.
- Unauthorized disclosure returns one closed result with no existence or reason
  oracle.
- The known-being inventory permits verification only. Matrix.org, Tribe
  Bridge and Cluster receipts cannot become relationship evidence.
- All published journey material is deterministic, synthetic and secret-free.
- The encrypted journey proves the local authorization→carrier→recipient
  intake→signed semantic receipt path. It does not claim external reachability,
  host deployment or the DM-071 cross-host canary.

## Verification disposition

- The DM-051–DM-055 plus DM-082 regression batch passes 106 tests.
- Strict typing passes for every source module changed by the completed
  relationship-delivery seam.
- The full source suite passes 539 tests with three explicitly gated skips.
- The clean installed wheel passes 420 package-lane tests with two explicitly
  gated skips; all 13 entry points answer `--help`.
- The installed DM-082 journey is byte-identical to its fixture and its report,
  checkout, wheel and sdist pass secret scans.
- Two independent builds are byte-identical. Wheel SHA-256 is
  `a699b153c374ead2b33520972dc5c03479bfa57c75116574983f3aa108f037da`;
  sdist SHA-256 is
  `f64fb0eed5a1bbdf57e4a64684da8d417d7de87838922173cad3dffed28ffbde`.

Installed conformance-twice remains the final local release gate after the
candidate commit supplies its source-commit identity. Public CI repeats every
gate rather than relying on this audit text.

Final operational boundary: the candidate performs no real relationship,
external disclosure, live route, host deployment, Cluster effect or service
cutover. Those remain separately authorized integration work.
