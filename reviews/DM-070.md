# DM-070 multihost convergence self-audit

Date: 2026-08-05

Verdict: **READY FOR PR AND CI; NO LIVE HOST OR REBIRTH CUTOVER
AUTHORIZED**.

## Scope and coordination

This audit covers issue #39 under accepted signed claim
`62395b1d-d74f-4c44-86c9-08bf6123dabd`, principal `codex@localhost`, branch
`dm-070-multihost-convergence`. CI plus this explicit adversarial self-audit
are the requested review gate; no recursive external review is required.

The candidate adds a deterministic installed-process acceptance journey and a
closed public evidence validator. It changes only `daimon-matrix` code,
contracts, fixtures, packaging, CI and documentation. Daimon Cluster remains
unchanged and is consumed only through an exact read-only provenance pin plus
the injected DM-037 body/fence/effect boundary. Matrix.org and Tribe transport
are absent.

## Authority findings

- One root manifest authorizes exactly two concurrently active embodiments,
  while credentials, incarnation lanes, signing/encryption/transport keys,
  capabilities, custody files, ledgers, sockets and peer journals stay
  disjoint.
- Partition healing is verified additive union of immutable per-origin chains.
  No arrival, label, time, path, length or hash rule elects an embodiment or
  rewrites the history into one global chain.
- Import remains distinct from adoption. Opposite local decisions and a later
  reversal converge as immutable evidence while effective projections remain
  observer-relative.
- A signed authority epoch retires Legion incarnation N and authorizes N+1.
  The old origin cannot write, the new lane starts at sequence 1 and previous
  high-waters remain unchanged.
- Cluster evidence controls only the named concrete resource and current
  holder/epoch. A stale effect replay and second holder fail; a different
  resource stays independent; ordinary signed Matrix life is unaffected.
- The historical cross-host canary and installed-host files are redacted,
  content-addressed attribution. Every identity, event, adoption and fence
  authority flag is false.

## Findings resolved during self-review

- The first successor manifest replaced the old incarnation row. It now
  retains that row as `retired` and appends the active successor, preserving
  explicit history rather than implying deletion.
- The first succession check compared ledger bytes before the authority epoch
  metadata had legitimately committed. The final test first applies the exact
  epoch, then proves an old-origin write is rejected without further mutation.
- High-water evidence initially read a private/obsolete field. It now checks
  the public `max_sequence` contract on both independently converged ledgers.
- Recreating the peer carrier after authority succession initially discarded
  the captured partition request. The final receipt retains and verifies that
  exact encrypted replay evidence before replacing runtime-only carrier state.
- The new Cluster provenance conformance evidence originally combined a local
  fail-closed check with an optional external checkout. In an installed
  conformance job this could report a required skip before CI created the
  checkout. The gates are now separate: the 86-scenario registry always runs
  the closed local verifier, while a dedicated CI step checks the exact Cluster
  commit and three file digests.
- Two new package modules changed the content-bound Hermes package inventory.
  The deterministic DM-041 provenance, profile, launch receipt and index were
  regenerated after the final registry pin.

## Durability and public boundary

- Two child processes load the production V3 bundle, acquire the ordinary
  single-writer lock, serve the authenticated AF_UNIX API and expose the
  bounded native encrypted peer HTTP handler. Only the synthetic clock and
  fixture custody are test-controlled.
- The journey exercises an ambiguous partition, exact ciphertext replay,
  response loss after receiver commit, restart before receiver commit, frozen
  page replay, bidirectional convergence and exact write-free no-op replay over
  ledger, exchange and outbox databases.
- `dm.multihost-convergence-receipt/v1` is closed, canonical,
  content-addressed, path-free and bounded. Cross-field validation rejects
  authority collapse, shared custody, relabelled directions/boundaries,
  observer collapse, old-origin acceptance, fence substitution, schedule
  drift, leaked private fields and winner metadata.
- The entry point accepts only an absent or empty owner-controlled state root,
  never overwrites its report, writes mode `0600`, and stops every exact child
  process in its cleanup boundary.

## Verification

- Final complete source discovery: **481 tests, OK; 17 documented
  external/private lanes skipped locally**.
- DM-070 focused source/package tests, including the exact pinned local Cluster
  checkout: **6 tests, OK**.
- Ruff format/lint, strict mypy over all 48 package modules and changed CI
  targets, `compileall`, `git diff --check`, JSON validation and
  DM-041/DM-042/DM-061/DM-070 generator checks: **clean**.
- Two isolated builds: **byte-identical and closed-allowlist inspected**.
  - sdist SHA-256:
    `df820edb5839c8b5a5010faa007c51630e4c5cdb3132425550a7451bb1610208`
  - wheel SHA-256:
    `b2b9f0d1a74492234238dddbda305a2b7a34c9f2487d38d878ae732b43ad7cc2`
- The installed wheel entry point produced a valid secret-free multihost
  receipt through two real child processes.
- Installed DM-026 conformance ran twice against the exact HMK pin: **86/86
  required scenarios, `release_ready: true`, byte-identical reports**.
  Canonical registry SHA-256:
  `f9ebedc99ef4d3a1bd3d80862600b8bffb19c695e85b0eae5766cefc258e7773`.
- Conformance report SHA-256:
  `8354a9302f25c97cfdd17fa8ada60e623f808dd9325f1033320d0925f2ae620e`;
  deterministic transcript SHA-256:
  `7338ed203b3017a9c993ee4741baf3a6c0fc0e676361756b1bedf0fd4e2850a6`.
- Secret scan covered the checkout, wheel, sdist, installed receipt and
  conformance report: **clean**.

Python 3.13 reports late cleanup warnings for pre-existing
`dm022-ledger-*` `TemporaryDirectory` objects after the successful unittest
summary. The final command exits 0; the warning paths originate in historical
DM-022 test fixtures, not DM-070 runtime state. CI remains the multi-version
gate.

## Residual operational boundary

DM-070 proves the current released architecture in isolated installed
processes and validates historical live evidence. It does not authorize or
perform a fresh Legion/daimonmatrix action, CompAII rebirth, body relocation,
production network change, provider call, real resource mutation or service
cutover. DM-071 still owns cross-being consent/root discovery and DM-078 owns
the fresh-host rebirth drill. Any live step retains its explicit human gate.

Final disposition: **READY FOR PR AND CI; NO LIVE HOST OR REBIRTH CUTOVER
AUTHORIZED**.
